import { Eye, EyeOff, Lock, Mail } from "lucide-react";

export const emailIcon = { email: <Mail size={16} /> };

export const eyeIcon = { eye_on: <Eye size={16} />, eye_off: <EyeOff size={16} /> };

export const locIcon = { lock: <Lock size={16} /> };
